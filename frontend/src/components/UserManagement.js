import React from 'react';

function UserManagement({ token }) {
  return (
    <div>
      <h2>Gestion des Utilisateurs</h2>
      <p>Ici, l'administrateur pourra gérer les utilisateurs (ajouter, supprimer, modifier).</p>
    </div>
  );
}

export default UserManagement;
